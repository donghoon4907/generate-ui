export enum InputSearchTabType {
  DEFAULT = "default",
  ICON = "icon"
}

export enum SelectTabType {
  COMMON = "common",
  SELECT = "select",
  OPTION = "option"
}

export enum ModalTabType {
  MODAL = "modal",
  HEADER = "header",
  BODY = "body",
  FOOTER = "footer"
}
