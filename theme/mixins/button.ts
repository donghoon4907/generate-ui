import { css } from "styled-components";

export const mixinBtnDefault = css`
  border: none;
  margin: 0;
  padding: 0;
  width: 100%;
  overflow: hidden;
  background: transparent;
  vertical-align: middle;
  text-align: center;
  appearance: none;
  user-select: none;
  color: inherit;
  cursor: pointer;
  font-size: inherit;
  font-weight: inherit;
  font-family: inherit;
  outline: none;
`;
